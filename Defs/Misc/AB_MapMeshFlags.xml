<?xml version="1.0" encoding="utf-8"?>
<Defs>

  <!-- Dedicated dirty flag for the sky-side below-things layer: lower-map
       thing/building changes mirror to exactly this flag on the sky drawer,
       so only SectionLayer_ABBelowThings regenerates (never the sky map's
       own content layers). -->
  <MapMeshFlagDef>
    <defName>AB_BelowThings</defName>
  </MapMeshFlagDef>

</Defs>
