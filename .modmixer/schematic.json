{
  "shortDescription": "Stacks up to 7 playable z-levels (3 up / 3 down) into ONE banded RimWorld Map joined by synthetic RegionLink wormholes, so hauling, needs, work and storage come free from vanilla. Levels and per-level size are bought from a fixed 146,000-cell budget. Per-colony climate bands make high levels genuinely alpine. Content on lower levels draws smaller once per level of drop, which is what makes a deep column read as depth.",
  "body": "# As above, So below II (V2)\n\n> V1 is DELETED. ~70 files, ~19,900 LOC. No em-dashes anywhere in shipped text; keep it that way.\n\n## Pitch\nUp to seven z-levels in ONE RimWorld `Map`. Priorities in order: (1) visual clarity from above, (2) performance, (3) vanilla parity.\n\n## Status\nVisuality, QOL, altitude/weather and settings passes are all VERIFIED IN PLAY. **Seven-level maps with a real basement generate clean in 5.5 s (run #196); a 4-band 3-up map in 3.4 s (run #200).** Below-snow rendering, the depth shrink (§6g) and the new-game camera anchor (§8a) are fixed but UNVERIFIED IN PLAY. **A four-pass code audit (§18) landed 2026-07-31: three state-release bugs, the eighth appearance of the descent bug, and seven optimisations. ALL UNVERIFIED IN PLAY.**\n\n---\n\n# 1. ARCHITECTURE\nOne `Map`, size `(w, 1, bandCount * Slot)`. Bands stack along +z from the deepest basement, with a permanently-fogged gutter between them. `Slot = ceil((bandHeight+2)/64)*64`; **64-alignment is load-bearing** (terrain shaders sample by world position). Per-level sizes 126 / 190 / 254.\n\n**The one insight.** `Region.Neighbors` is *topological, not spatial*. Wormhole `RegionLink`s between one-cell `RegionType.Portal` ends conduct connectivity without merging rooms, temperature or vacuum, and `Reachability.CanReach` works unpatched. But reachability is not a path: 1.6's pathfinder is jobified and cannot traverse a synthetic link, hence segmented movement (§3).\n\n**Hard 1.6 constraints.** The path grid is an IJobParallelFor over EVERY cell, so total stacked cells is the runtime metric. No `PathFinder.FindPath`. `BlocksDiagonalMovement` trips on unwalkable cells.\n\n**⚠ The stack is 2x to 7x anisotropic.** Anything vanilla normalises by BOTH map dimensions is distorted, and that bug class cannot appear on an ordinary square map.\n\n### ⚠ THE SLICING RULE, six shapes\n| Shape | Example | Fix |\n|---|---|---|\n| Anchor, a point picked as a fraction of `Size.z` | river and lake centre; random/edge cell (§15a) | remap the point |\n| Field, a gradient centred on the map centre | coastline (§6e) | re-centre the sample coordinate |\n| Noise space, a slice is not a short map | Map Preview (§6d) | generate at full height, then crop |\n| Distance-to-extent, a margin measured off `Size` | `CloseToEdge`, no-build edge (§15b, §15d) | re-base onto the cell's own band rect |\n| Aspect ratio, normalised by BOTH dims | water surface (§6b-LAKE) | none, cosmetic, accepted |\n| **Map-wide scalar**, one per-tick value for the whole map | snow melt (§7), temperature readout (§16) | rewrite it per cell at the consumer |\n\nClassify a new bug into one of these six, but PROVE it. Still unaudited: Basin, Cavern, Chasm, Cliffs, Hollow, Valley (all use `FalloffAtAngle`).\n\n# 2. LEVEL PLAN, budget 146,000 cells, FIXED\n`4 x 190 = 145,920` sets the figure. `5 x 190` and `3 x 254` are refused. `ABMapSizeLimit` owns it. **`7 x 126 = 112,896` is VERIFIED IN PLAY (run #196).**\nPer-level size is SQUARE, which is why `GenStep_Scatterer.CountFromPer10kCells` (it squares `Size.x`, not `map.Area`) already yields a per-BAND count. Only `GenStep_AnimaTrees` and `GenStep_TerrainPatches` read `map.Area`.\n\n**⚠ The level chooser is also the lab.** `0 above / 0 below` generates an ORDINARY single-level map, the vanilla control. **STILL UNVERIFIED: does `0 up / 0 down` actually reach generation?** Pre-flight check: the strip must read 1 level and 36,100 cells.\n\n**⚠ The chooser UI: own the widget, do not patch the data.** `Dialog_AdvancedGameConfig.MapSizes` is `static readonly` and the reflection swap SILENTLY FAILED in play. Now our own radio buttons write `mapSize` directly. Window grown via an `InitialSize` postfix.\n**⚠ The chooser LATCHES global state, and the release is a `Finalizer`.** `BeginChooser` sets `inChooser` (which gates a patch on `Listing_Standard.RadioButton`, a primitive every mod's settings window uses) and swaps the static `MapSizes` array. That release used to sit in the Postfix's `finally`, which does not run when VANILLA throws - see §18a.\n**The level spinners are NOT known to be broken.** A `3 up / 0 down` map generated when `0 up / 3 down` was intended in run #190; that was USER ERROR, confirmed. Do not re-open without new evidence. **The plan PERSISTS between colonies**, so an untouched chooser silently reuses the last layout, which is the likeliest explanation for any \"wrong plan\" report.\n\n# 3. CROSS-BAND MOVEMENT\nSegmented at ONE interception: `StartPath` prefix, tick sweep, `Carry` re-issues `StartPath`. `ABWormhole.TryGetTransit` BFSes the band graph; hops must strictly decrease. **Multi-hop routing STILL UNTESTED IN PLAY.**\n\n# 4. GROUP ORDERING\n`RecomputeDestinations` spreads the selection along the drag line, which between bands runs through the GUTTER. The odd/even signature (\"3 and 5 fail, 2 and 4 do not\") is the proof, because only ODD counts sample exactly 0.5. Fix normalises `start`/`end` into the commanded pawns' band. Verified.\n\n# 5. ⚠ ONE DESCENT RULE: `ABBands.TryResolveVisibleBelow`\nDescends while each level is see-through, stops at the first opaque floor or the bottom band, returns the accumulated drop. TEN systems share it. **EIGHT separate appearances of the single `- Slot` bug** (the eighth found by the §18 audit, in the lighting overlay, written as INDEX arithmetic so the standing `grep '- Slot'` audit could not see it).\n\n**`ABBands.TryResolveVisibleFrom` is now THE gate**, and it is what most consumers should call. It folds the four-question preamble every caller was hand-rolling - in bounds, band > 0, out of the gutter, own terrain see-through - onto the descent, plus an optional unfogged check. Six passes are on it (lighting, snow, watergen, sun shadows, edge shadows, click-through). Two legitimate exceptions remain: `SectionLayer_ABBelowV2` needs the \"in bounds but fogged\" distinction to emit its air mask and fog fan, and the upward-shape consumers (`ABBelowDynamicDraw`, `ABBelowOverlays`, `ABBelowMultiSelect`, `ABBelowSelectionDraw`) ask a different question - \"is THIS thing what the column shows\" - and translate before descending.\n\nSun-shadow subtlety: its mask is neighbour-based, so mask and descent are answered together by `TryResolveDropAround`, taking the smallest drop among the nine cells.\nStill single-band AND unshrunk: the realtime-things pass in `ABBelowDynamicDraw` (motes, fire, frames, projectiles).\n\n# 6. SKY-MASS PRESENTATION (verified)\nFour rejected attempts on record: force-linked wall masks, high-queue cover, wall-sprite suppression, and theorising twice about \"flat\" mountains before running the below-layer report.\nDespeckle (walls dissolve only at 7+ open neighbours; at 6 it ate the outermost wall row off every meadow edge), one central fog hole, five-terrain plateau palette, cliff faces, alpine pass.\n**⚠ The mass tapers with altitude, so +3 may contain NO LAND AT ALL.** Check for ground before diagnosing anything about the top level. Independently confirmed by timing in §9e.\nOre glyph bug: ore defs have no `naturalTerrain`, so a \"can't happen\" fallback drew the whole atlas sheet per cell.\n\n# 6a. ⚠ THE BELOW VIEW IS A SET OF MIRRORED PASSES, NOT ONE RENDERER\n`SectionLayer_ABBelowV2` is a faithful port of vanilla `SectionLayer_Terrain` **and nothing else**. Every OTHER vanilla pass that contributes to how ground looks needs its own mirror, or that contribution is simply absent from the below view. Confirmed twice:\n- **Water** (§6b): `SectionLayer_ABBelowWatergen`.\n- **Snow** (§6f): `SectionLayer_ABBelowSnow`.\nBefore adding a below-view feature, ask which vanilla SectionLayer draws it. Existing mirrors: BelowV2 (terrain + things + fog fan), BelowLighting, BelowShadows, BelowEdgeShadows, BelowWatergen, BelowSnow, MountainCap.\n\n**Cross-band invalidation is solved by `Patch_MapDrawer_ABMirrorDirtyUp`, and it now obeys an ASCENT RULE.** It mirrors every dirty flag verbatim to bands above (one step was not enough once levels stacked; symptom was \"ground floor mineables disappear on floor 2\"), so a new mirrored layer costs one `relevantChangeTypes` line rather than a new propagation path. **But it used to climb to EVERY band unconditionally, and that is the inverse of §5.** A band can only show a cell if every level between them is see-through; the first opaque floor hides it from that band and all above. The climb now stops there, tested over the cell AND its eight neighbours (the mirrored passes are neighbour-sampling: edge fades, fog fan, snow kernel, shadow caster mask). The FIRST step stays unconditional because `SectionLayer_ABMountainCap` derives from exactly one Slot below regardless of transparency. The patch also mirrors vanilla's own `ProgramState != Playing` guard - see §18b.\n\n**⚠ THE UNIT OF WASTE HERE IS A SECTION REGENERATION, NOT A CELL.** Seven of our layers plus vanilla's ten rebuild per dirtied section, so an unbounded mirror multiplied every mined rock by bandCount. Steady-state fps never showed it; it lands entirely in the hitches during mining and construction.\n\n**⚠ SIX PASSES SHARE THE BELOW VIEW.** Any future per-frame geometry transform must be applied to ALL of them from one definition, or lighting / shadow / water / snow shear off the terrain they belong to. That coupling is what made Perspective Mode expensive to own (§6g).\n\n# 6b. ⚠ WATER: 1.6 water is a TWO-PASS SCREEN-SPACE effect\n`SectionLayer_Watergen` (vanilla, `internal`, cannot be subclassed) is `SectionLayer_Terrain` plus `waterDepthMaterial`, drawn to the WaterDepth SUBCAMERA's layer. The visible `Map/TerrainWater` material samples that texture at its own SCREEN position.\n- Water missing from upper levels, FIXED: `SectionLayer_ABBelowWatergen` overrides `DrawLayer` to target `SubcameraDefOf.WaterDepth.LayerId`.\n- Rivers not generating, FIXED: remap `GetRiverCenter` (not `Init`, which consumes it immediately). `GetLakeCenter` needs the same patch or it undoes the first.\n\n# 6b-LAKE. CLOSED: surface streaking accepted as vanilla appearance\nPROVEN in run #173: a tile with water but no water mutators still streaked. **The ghost-river theory is dead; do not revive it.** Found en route: vanilla's flow builder ends `zero /= (float)num3` with no zero guard, writing NaN into a bilinear-filtered texture.\n\n# 6c, 6d, 6e. COMPAT AND COASTS (all verified)\n**Dubs Mint Minimap:** ONE RECT, not seven patches. `DrawTextureFitted` preserves the TEXTURE's aspect, so use `ScaleMode.StretchToFill`. Reflection only.\n**Map Preview:** never calls `MapGenerator.GenerateMap`. Generate at full stacked height and crop via `TexCoords`. `MaxMapSize.z` MUST rise (default 500, a stack reaches 896).\n**Coasts:** CENTRED wrong, not scaled wrong. `z' = (z % Slot) + (map.Size.z - bandHeight) / 2`, NO SCALING. Nine coastal mutators, none override `GetNoiseValue`.\n\n# 6f. BELOW-SNOW (`SectionLayer_ABBelowSnow.cs`, UNVERIFIED IN PLAY)\nSnow lying on a lower level was invisible from above. Not masking, not translation, not the descent rule: **no mirror of `SectionLayer_Snow` existed**, so below-snow was never emitted.\n**The near-miss that hid it:** `CellTerrainAt` genuinely samples `map.snowGrid.GetDepth(below)` from the correct cell, but `CellTerrain`'s snow coverage is consumed only by `MaterialFor` to pick the POLLUTED terrain variant. The right value was read for an entirely different purpose, which is the kind of thing that survives review.\n**Why it was simpler than the terrain mirror:** vanilla's snow layer emits no per-cell geometry. It builds ONE solid 9-verts-per-cell grid over the section and writes only vertex COLOURS, depth in alpha. That grid is positional and our section already sits in the viewing band, so nothing needs translating; only the sampled cells move. No vertex fix-up, no `- Slot`.\nDetails: geometry built once and reused (NOT `ClearSubMeshes(All)`, unlike every other AB layer, or colour count stops matching vertex count); iteration order must match `MakeBaseGeometry` (x outer, z inner, nine per cell); cells with nothing below still emit nine transparent vertices or all later colours shift; altitude matches the below-terrain print (`TerrainScatter`) to reproduce vanilla's own terrain/snow relationship; all nine kernel samples taken at the CENTRE cell's drop, because mixing two bands into one smoothing kernel would seam.\n**⚠ Open risk:** draw ordering is queue-decided, not altitude-decided. `MatBases.Snow` may need queue work to sit above below-terrain and under below-things. Expect one iteration, as water did.\n\n# 6g. DEPTH SHRINK (`ABDepthView.cs`, UNVERIFIED IN PLAY)\nThe below view was geometrically correct and read as **flat levels stacked in a list**. Restores V1's `belowThingScale`, generalised: V1 only ever had ONE level below so a flat 0.85 sufficed, whereas here the exponent is the accumulated drop from `TryResolveVisibleBelow` (three levels down draws at 61%). Baked into vertices at print time, hence free per frame and unable to slide relative to the ground. DEFAULT ON, slider 60-100%.\n- **Printed things:** `FinishNewVerts` does shrink-about-`TrueCenter` and the `drop` translate in ONE pass, scale first, so the pivot is the real centre and there is no second home for the `- Slot` bug family.\n- **Live pawns:** `Patch_PawnRenderer_ABBelowShrink` right-multiplies `PawnDrawParms.matrix`. Right-multiply is load-bearing (local space, so the pawn shrinks about its own draw root; a left multiply slides every below pawn towards the map origin). Finding that ONE funnel is why this is two small patches instead of a per-piece hunt.\n- **⚠ The atlas trap, ported from V1:** past `ZoomRootSize` 18 humanlikes render from a cached atlas blit that never reads `PawnDrawParms.matrix`, so the scale silently evaporates at exactly the zooms used to look down a column. `Patch_PawnRenderer_ABBelowDisableCache` forces `disableCache` for below pawns only. **Any in-play verification must cross that boundary in both directions.**\n- **⚠ V1's exclusion filter kept verbatim, both halves paid for:** LINKED graphics print one quad per cell, so per-cell shrink dots a wall into a dashed line; natural rock is excluded BY DEF (`mineable` / `isNaturalRock`) not by link type, because Better Mountains swaps rock to non-linked `Graphic_Random` wholesale and tore the mountains into a gappy field (V1 run #50).\n- Baked, so a settings change needs `RegenerateEverythingNow` - done once in `ABMod.WriteSettings`, not per slider frame.\n- **Not tinted.** V1 dimmed as well; V2 has `SectionLayer_ABBelowLighting`, so a tint on top is the double-darkening that made the sky view murky. Size is a distance cue that costs no brightness, which is why only it came back.\n- **Realtime non-pawn things are NOT shrunk** (frames, projectiles): `DynamicDrawPhaseAt` takes a position and nothing else. Same corner as §5's single-band gap; both want the same fix.\n\n**⚠ PERSPECTIVE MODE WAS BUILT AND REMOVED. Do not re-add it as polish.** A camera-anchored contraction of the whole below view (`p' = eye + (p - eye) / (1 + k)`) shipped briefly as an off-by-default toggle, applied per frame as a `Matrix4x4` to `Graphics.DrawMesh` - the technique vanilla itself uses in `MapDrawLayer_OrbitalDebris.DrawLayer`, so it was genuinely free. It worked. It was cut on the user's call once the shrink was seen in play: **the shrink alone already carried the depth read**, and perspective bought a second cue at the price of a real artifact (no occlusion exists between a band's floor and the level below, so content slid a fraction of a cell out from under the near lip of its opening towards the screen edges). Re-opening needs a NEW reason - something the per-object shrink provably cannot express - and would have to re-solve the six-pass coupling in §6a, which was the expensive part.\n\n# 7. PER-LEVEL CHARACTER\nOre `x(1 + 0.45*(depth-1))`, cave openness `x(1 + 0.35*(depth-1))`, both applied AFTER the settings clamp.\n**⚠ The snow line never worked, and the comment saying it did not need to was wrong.** `SteadyEnvironmentEffectsTick` computes `outdoorMeltAmount` once per tick for the whole map and never calls `TryGetTemperatureForCell`. Dead for months behind a confident, wrong sentence.\n**Climate defaults** (per colony, §17): +1 = -7, +2 = -16, +3 = -35, basement flat -6, wind x1.15 / x1.30 / x1.50. **No ceiling clamp by design.**\n\n# 8. CAMERA\n`ABCameraBounds.For(level)` keyed by LEVEL, not band index. `maxZoom` left DERIVED. `panMargin` 63.5 on every level. `Dialog_ABCameraCalibration` opens with the clamp UNLOCKED.\n**The clamp and the level-switch input share `ABGuard.Camera`.** Both sit on per-frame / per-GUI-event paths, which is exactly where an unthrottled `Log.Error` turns a cosmetic bug into an unplayable game (§18a). Consequence to know: if the clamp trips, band switching stops too. The view clip and the curtain are NOT gated on it, so other bands stay hidden either way and the degradation is graceful.\n\n# 8a. NEW-GAME CAMERA ANCHOR (`ABBandView.LandOnColony`, UNVERIFIED IN PLAY)\nReported: \"sometimes the camera does not land on the colonists as it does in vanilla.\"\n`Game.InitNewGame` ends with `JumpToCurrentMapLoc(MapGenerator.PlayerStartSpot)`. That is correct on an ordinary map because nothing moves the colony after the spot is chosen. On a banded map two things do, BOTH after the pawns are already standing on the ground: `RescueStrandedColonists` relocates anything the pod scatter threw across a gutter, and the carve can make the recorded cell unstandable. The camera then obeys a cell nobody is on - intermittently, because it depends on where the scatter landed.\n**Confirmed mechanism in run #200:** `DropThingsNear failed to find a place to drop Nyx near (153, 0, 24). Dropping on random square instead.` A random drop spot on a banded map can be any band or the gutter; §15a's `GenSpawn.Spawn` prefix catches it, but the recorded start spot never learns about the move. (Run #190's unexplained `DropThingsNear (59,0,476)` vs start spot `(63,0,63)` is the same thing, and is no longer unexplained.)\nFixed in two places on purpose:\n- **Upstream:** `FixPlayerStartSpot` now prefers the colony's ACTUAL position (free colonist nearest the group centroid, restricted to the surface band) over the recorded plan. Fixes every post-generation consumer, not just the camera.\n- **Backstop:** `ABGameComp.StartedNewGame` calls `LandOnColony`. **⚠ That hook, and only that hook, runs after vanilla's own camera jump** - a `GameComponent.FinalizeInit` hook is overwritten a few lines later. `Game.FinalizeInit` flips `ProgramState` to `Playing` before it, so `CameraJumper` does not no-op (it returns silently otherwise).\n- `rememberedCameraPos` is written by hand too, because `ABBandMap.FinalizeInit`'s deferred load-path fixer judges by that field and would otherwise immediately undo this.\nAims at the pawn NEAREST the centroid, not the centroid: with pods scattered around a lake the mean can be a cell nobody is near.\n**⚠ OPEN, AND NOT YET DISTINGUISHED:** in #200 the start spot was `z=24` on a surface band spanning z 0-189, i.e. EXACTLY the boundary row of the `PodScatterMargin = 24` safe strip - which is close to the worst case for a finder that scatters beyond the margin. Vanilla's own pick landing there (my clamp accepts it untouched) and my clamp pinning it there look IDENTICAL from outside. Verbose logging prints the chosen spot and the one it replaced; run one colony with it on before touching the margin.\n\n# 9. GENERATION COST: DIAGNOSED AND LARGELY FIXED (runs #188, #190, #196, #200)\n\n## 9a. The profile stamps its own conditions\n`ABGenProfile.Report` prints `TILE` (id, biome, hilliness, elevation, temp, rainfall, coastal, mutators), `PLAN` (bands, height, surface band, up/down, doomed cells), a `SCOPING:` toggle line, and two censuses (`DESTROYED by def`, `SUPPRESSED by def`). Added because four earlier carve timings lacked the tile and are uninterpretable. It caught a changed tile on its FIRST use and voided a comparison that would otherwise have been believed.\nIt is emitted at WARNING level, which is load-bearing: single-occurrence info is filtered before it reaches the monitor, so a profile logged as a message would be invisible.\n\n## 9b. ⚠ THE COST IS DESTRUCTION, NOT GENERATION\nRun #188, tile 846, TemperateForest/Mountainous/Mountain+Caves, 190x768, 4 bands:\n| Phase | ms |\n|---|---|\n| All 42 gensteps (wall) | 3,972 |\n| AB band carve | **20,447** |\n| of which `Sky clear` | **20,126** (98.4%) |\n| `Map.FinalizeInit` | 169 |\n| things destroyed | 74,584 |\n\nGeneration is 16%, destruction 84%. **The original \"scoping saves both halves\" framing was WRONG.** Per-destroy price is 0.19 to 0.27 ms and STABLE across runs and tiles, so **op count is the only lever**. Mechanism is `ListerThings` removal, a linear `List.Remove` over ~100k-item lists, hence superlinear savings.\n**CLOSED:** carving before `FinalizeInit` is worth at most 169 ms of 24,600.\n\n## 9c. The culprit was rock, proven by census\nRun #190: `SUPPRESSED` 43,517 rock vs `DESTROYED` 4,939 total of which only 238 rock. Rock moved columns, 99.5% of it. Would-be total 48,456, so **rock was 90% of everything the carve deleted**.\n**Reproduced on a third tile (run #200, BorealForest/Mountainous, 190x768, 3 up / 0 down):** 50,436 suppressed (39,604 Limestone + 10,824 Slate) against 3,825 destroyed, of which only 364 rock. Gensteps 2,175 ms, carve 1,243 ms, total ~3.4 s. The ratio is now stable across three tiles and two biomes.\n\n## 9d. SHIPPED: `ABDoomedBands.cs`\nSuppresses `GenSpawn.Spawn(ThingDef,...)` inside `GenStep_RocksFromGrid` for doomed SKY band cells. Safe because: **shape** (per-cell noise test, no count redistributed, same licence as the plant skip); **state-neutrality** (only the spawn is cancelled, roofs / roof cleanup / `ScatterLumpsMineable` all still run, and `ClearCellHard` never touched roofs, so `ABSkyBandGen` gets bit-identical state); **sky only**. Patched on the `ThingDef` overload so the cancel precedes `ThingMaker.MakeThing`.\n\n## 9e. Run #196: the first basement, and the bottleneck moved\nTile 54863, Desert/Mountainous/Mountain+Caves, 126x896, **7 bands, surface band 3, 3 up / 3 down**. Gensteps 2,289 ms, carve 3,180 ms, total ~5.5 s for SEVEN levels.\n```\nFillRock  bands 0,1,2   827.7 + 933.3 + 845.4 = 2,606 ms  (82% of carve)\nSky clear bands 4,5,6   168.6 + 120.0 +  12.4 =   301 ms  ( 9%)\n```\n**The sky side is solved.** The basement is now the whole cost.\n**⚠ MY OWN §9d CAUTION IS FALSIFIED.** I argued against suppressing vanilla rock in basement bands because `FillRock` keeps it. Run #196 gives the hit rate for the first time: 47,628 basement cells, 40,755 fresh spawns, so **the keep hits only 6,873 times (14.4%)** (vanilla only places rock where elevation > 0.7, and the def must match too). Rock destroyed in the basement: 5,309 Granite + 1,804 Slate = **7,113**. Suppressing there trades 7,113 destroys (~1.4 s) for 6,873 extra spawns (~200 ms), because destroys cost roughly 7x spawns inside the generation window. **Net ~1.2 s, about 38% of the remaining carve.** The shape of the reasoning was right; the magnitude was never checked, because no basement had ever run.\n**Sign flip, a confirmation not a bug:** with rock suppressed, `Sky clear` per band INVERTED (125/415/528 rising) versus #188 (7517/7029/5580 falling). Rock tapers upward (§6), so higher bands have more OPEN ground, and chunks/rubble only grow on open ground. Run #200 shows the same rising shape (58 / 320 / 392).\n**⚠ `CavernGen` read 0.9 / 0.0 / 0.0 ms** on a tile carrying the Caves mutator. Either the basement genuinely has no cave systems or openness is at zero. UNDIAGNOSED.\n**FALSIFIED, still dead:** \"band 1 is consistently ~3x bands 2-3.\"\n**Do not blanket-skip** water or coast mutators; §6b, §6e, §6d depend on them.\n\n## 9f. Next targets, ranked\n1. **Basement rock suppression** (§9e), ~1.2 s, the largest remaining item.\n2. **`GenStep_RockChunks`**, the dominant survivor on sky-only maps (`Filth_RubbleRock` 2,193 of 3,825 in #200, 3,183 of 4,939 in #190). Verified per-cell probabilistic, passes the shape test.\n3. **Scattered ruins, also a CONTENT BUG.** Deleted from doomed bands: `Wall`, `AncientFence`, `AncientGiantWheel`, `Filth_MachineBits`, `Steel`. Per §2 a scatterer's count is sized for ONE band but placement spans the whole stack, so **the surface is losing a large share of the ruins vanilla intended**. Fixes content and cost together. ⚠ Paired risk: `AncientExostriderRemains could not find cell to generate at` (the §15b starvation signal) has now fired on TWO different tiles, runs #190 and #200 - reproducible, not a one-off. The derived seam margin must be re-checked at the same time.\n4. **`ABRockGen.PickIndex` evaluates every rock type's 6-octave Perlin per cell** (argmax over static noise fields), ~650k noise samples per 190-band, and recomputes for wall cells again in `ABSkyBandGen`. Precompute a `byte[]` rock-index grid per band, or cut the octave count - a rock-boundary field does not need six. NOT DONE: it changes generation determinism, so it wants its own run.\n\n# 10, 11. PERF AND LINKS\n59.6 fps at 96 pawns, mod approximately 2% of frame. All rendering is bake-time section-mesh geometry. Elevator SHIPPED but untested.\n**⚠ THAT BENCHMARK MEASURES THE WRONG THING, and §18 is why.** This architecture's cost is concentrated in section-regeneration BURSTS, not steady state. A colony standing still never triggers the dirty mirror; a colonist mining a 20x20 room with three sky levels open triggers a section rebuild per band. Any future perf claim needs a mining/construction test with a multi-level plan, watching the frame graph, not the average.\n\n# 12. DIAGNOSTICS\nBand info, below layer and pawn reports, water report, transit health, stuck probe, lighting/shader/biome reports, bisect toggles, ABGenProfile, camera calibration.\n**⚠ Bisect toggles PERSIST in the pinned palette across runs.** Run #172 opened with a stale `belowWater=False` that mimicked a fixed bug exactly. Scoping toggles are now STAMPED INTO THE PROFILE for this reason.\n**⚠ Poll the monitor even when nothing auto-prompts.** Single-occurrence `[RimWorld]` warnings are filtered from summaries. Confirmed in #190 and again in #200, where the ONLY auto-prompt was our own generation profile and `monitor_poll` turned up the two warnings that actually mattered - one of them (`DropThingsNear`) the direct confirmation of §8a's mechanism.\n**⚠ The auto-prompt is edge-triggered per run.** A second occurrence of the same log call site never re-prompts; use `monitor_get_error` (returns the LATEST occurrence) or watch the `×count`.\n`AB2: band info` is the fastest ground truth for \"what did this map actually generate as\"; it settled a level-plan question in one line after three messages of theorising.\n**Kill switches are a diagnostic surface, so every switch must have a call site.** Twelve of nineteen had none (§18c). The settings panel renders `ABGuard.AllSwitches`, so dead switches are rows that can never change state - which makes the readout useless exactly when it matters.\n\n# 13. OPEN WORK\n1. **Verify the §18 audit in play.** Highest value: the dirty-mirror ascent rule (a stale below view would show as \"mineables disappear on floor 2\" again) and the lighting descent fix (deep views should now darken at night instead of staying bright). Test with 3 sky levels: mine a large area on the surface, watch for stale sections, then check night lighting from +3.\n2. **Verify the depth shrink in play** (§6g). Needs a populated level below the viewing level, and must be checked at BOTH sides of `ZoomRootSize` 18 - that boundary is the atlas trap, and the failure looks like the shrink flickering off rather than like a missing patch.\n3. **Verify the new-game camera anchor** (§8a), and separate the two readings of the `z=24` start spot with verbose logging on.\n4. **Verify below-snow in play** (§6f). Needs a snowy or cold tile with a populated level beneath the viewing level. Queue ordering is the expected failure mode.\n5. **Basement rock suppression** (§9e/9f), ~38% of the remaining carve.\n6. **`CavernGen` did nothing in the basement** (§9e). Three levels of solid rock with no caves is plausible but unconfirmed.\n7. **⚠ A banded generation produced NO profile at all.** The 126x512 map in run #190 generated as banded yet `ABGenProfile.Report` never emitted, though the same call site fired earlier in the run. `Report` is unconditional and no error appeared. Unexplained. (It fired normally in #200.)\n8. §9f items 3 and 4 (ruins content deficit + scatterer starvation; Perlin precompute).\n9. **Audit the other `FalloffAtAngle` mutators**: Basin, Cavern, Chasm, Cliffs, Hollow, Valley.\n10. **Never exercised in play:** multi-hop stair router, elevator, reload fix. Basement GENERATION verified (#196); basement PATHING and wormholes still are not.\n11. **Below-pawn drawing is serial and cache-disabled** (§18d). The largest remaining steady-state item, and the only one left.\n12. Realtime-things pass still single-band AND unshrunk (§6g/§5). Selection bracket drifts upward as you zoom out. Waterfalls, combat never verified, vertical conduit, **store page still says three levels**.\n13. **`ShotReport` cross-band accuracy silently drops target size.** `Patch_ShotReport_ABCrossBandDistance` rewrites a Thing target to a CELL target so the localized distance survives, which loses `TargetSizeFactor` and thing-derived cover. Documented in the file, not fixed - a proper fix needs a transpiler.\n\n# 14. LESSONS\n**Method.** Prove a subsystem innocent by finding a state where the suspect CANNOT EXIST. A documented rejection may be re-opened only by naming the confound that invalidated it. Change one thing per experiment. Run the diagnostic before the third theory. An exact number is a diagnosis. Use the product's own knobs as lab equipment. A beautiful fit is not evidence. Knowing when to stop is a result.\n**⚠ Measure the ratio before optimising the half you assumed.** Generation was 16%, deletion 84%. The fix was right, the reason was wrong, and only the profile said so.\n**⚠ A census beats a magnitude.** \"74,584 things\" is not a diagnosis; \"43,517 Granite and Limestone\" is. Pair a SUPPRESSED census with a DESTROYED one so the two are a conservation check, and the saving can be computed from ONE run without a matched A/B.\n**⚠ Stamp the experiment's conditions into its own output.** The tile stamp voided a bad comparison on its first run.\n**⚠ Evidence consistent with both hypotheses is not evidence.** Twice now. \"The size selection stuck, so the spinners must be broken\" was wrong: an untouched spinner reusing a persisted value looks identical. And a start spot sitting exactly on the safe-strip boundary looks the same whether vanilla chose it or my clamp pinned it there (§8a). Ask what a datum looks like under the OTHER hypothesis before spending it - and if you cannot tell, say so and go get the log line that can.\n**⚠ An optimisation you declined for a stated reason still owes you the number.** The basement keep was left alone on the reasoning that it saved work; when a basement finally ran, the hit rate was 14.4% and the decision inverted. A conservative choice is still a claim.\n**⚠ When a symptom says \"X is missing\", ask how many PASSES X takes.** Twice: water (two passes, screen-space) and snow (its own SectionLayer). A below view that mirrors one vanilla layer mirrors only that layer.\n**⚠ A correct value read for the wrong purpose looks like coverage.** Below-snow was sampled correctly and used only to pick a pollution variant. Grep for the value's CONSUMER, not just its read site.\n**⚠ A deleted feature's exclusion list is the expensive part.** Restoring V1's below shrink was fifteen lines; the value was in the two exclusions (linked graphics, rock-by-def) and the atlas-cache trap, each of which had cost a run to find. Read the OLD code before rewriting the old feature.\n**⚠ Two cues for one property is one cue too many.** Perspective Mode was correct, cheap and cut anyway (§6g): the shrink already carried the depth read, so the second cue only added an artifact and a six-pass coupling to maintain. Cheap to build is not the same as free to own. Build the smaller cue first and check whether it is sufficient BEFORE building the larger one.\n**⚠ When two hooks both \"run at startup\", find the one that runs after the thing you are fixing.** The new-game camera anchor is inert in `GameComponent.FinalizeInit` and correct in `StartedNewGame`, for the single reason that vanilla's own camera jump sits between them.\n**⚠ Aim at the fact, not at the record of the intent.** `PlayerStartSpot` is a plan; the pawns are where the colony IS. Anything that reads a stored target after other code may have moved the subject should re-derive it.\n**⚠ A rule enforced by grepping for a SYNTAX only catches that syntax.** The one-descent rule was audited by grepping `- Slot`. Its eighth instance was written as index arithmetic (`idx - slot * sizeX`) and sailed through every sweep. A rule holds when the correct version is the only CONVENIENT one, not when there is a checklist for the wrong one - which is why `TryResolveVisibleFrom` now exists.\n**⚠ Duplication is how an invariant dies.** Seven copies of the see-below preamble, each with a slightly different subset of the checks, is exactly the soil the eighth descent bug grew in. Count the copies before trusting that a rule is enforced.\n**⚠ A comment is not an invariant.** \"Each callee self-guards\" was true of three files and enforced by none. If the contract matters, spend the try/catch.\n**⚠ A kill switch with no call site is not a safety net, it is noise.** Twelve of nineteen. They rendered in the status panel as subsystems that could never change state.\n**⚠ Steady-state fps hides burst cost.** 2% of frame at rest said nothing about a mirror that multiplies every mined rock by bandCount. Benchmark the ACTIVITY that triggers the work, not the idle state.\n**Claims versus facts.** A comment asserting why something is safe to delete is a claim (§7). Re-measure the assumption you are about to build on. Verify a patched branch is reachable before debugging your own logic.\n**Shapes.** Classify into one of the six. A slice of a tall map is not a short map. A uniform symptom is a scale or constant, a patchy one is data. An odd/even symptom is arithmetic.\n**Patching.** When N call sites share ONE linear mapping, re-aim the shared parameter. Find the ONE virtual everything funnels through (`PawnDrawParms.matrix` is the pawn-rendering example). Patch the method that PRODUCES a value, not the one that stores it. Patch the idempotent READ, not the cached WRITE. Own the widget instead of patching the data it reads. Never put a foreign type in a patch-class signature. **Suppress the callee, not the caller**, when a genstep does several jobs and only one is waste. **State taken in a Prefix is released in a Finalizer, never a Postfix** (§18a). **Mirror the guard of the method you postfix** (§18b).\n**Engine facts.** `TerrainDef.holdSnowOrSand` defaults TRUE. Draw order inside and across SectionLayers is settled by material render QUEUE, not by altitude. Altitude is draw order for things, and `WorldClipper` sorts above `Skyfaller`. A null global texture falls back to the shader's default. Vanilla divides by a count without guarding zero. A spatial helper that reaches 22 rows will cross a 2-row gutter. `Banded` is FALSE for the whole of map generation, so generation-time patches must read the pending layout. `SectionLayer` subclasses are auto-discovered by `Section`'s constructor, on EVERY map. Any type with a `Material` field needs `[StaticConstructorOnStartup]`. Statics keyed by game-scoped ids leak; use a CWT. FinalizeInit runs on a worker thread. Ore defs have no `naturalTerrain`. `CameraJumper.TryJump` silently returns unless `ProgramState == Playing`. **`MapDrawer.MapMeshDirty` also returns unless `ProgramState == Playing`.** `MapDrawLayer.DrawLayer` takes a per-submesh matrix, so a per-frame geometry transform costs nothing (vanilla's own `MapDrawLayer_OrbitalDebris` is the precedent). Humanlike pawns past `ZoomRootSize` 18 render from an atlas blit that ignores `PawnDrawParms.matrix`. **Gensteps run with live-map bookkeeping DEFERRED, which is why the same work costs ~40x after `FinalizeInit`.** Harmony skips POSTFIXES when the original throws, but always runs FINALIZERS.\n\n---\n\n# 15. QOL PASS (verified, run #172)\n**15a. Nothing may exist in the void.** `ABBandSafety.cs`. Finders answer inside the surface band; a prefix on the 7-arg `GenSpawn.Spawn` rewrites `loc`; a 120-tick sweep catches teleports and pre-fix saves. Always relocates to the SURFACE. Scoped to pawns and the gutter only.\n**15b. Structures must not straddle a seam.** `GenGrid.CloseToEdge` measured against `map.Size`, so the surface band reported itself as inland everywhere. One prefix re-bases onto the cell's own band rect; eleven cold call sites inherit it. Backstop: postfix on `GenStep_Scatterer.CanScatterAt`. Seam margin DERIVED (`bandHeight/24`, clamped 4 to 12); a flat 10 starved scatterers on small bands. **Starvation still observed on two tiles - see §9f item 3.**\n**⚠ ONE surface-edge finder, `ABBandSafety.TryRandomSurfaceEdgeCell(map, Rot4? dir, ...)`.** There were two, in different files behind different helper classes, because the dir and no-dir vanilla overloads were band-corrected at different times. A null dir means \"any side, re-rolled every attempt\" - preserved deliberately, because a validator that rejects a whole side must not trap the search on it. `ABBandEdges` forwards.\n**15c.** `panMargin` 63.5 on every level.\n**15d. Vanilla's no-build map edge REMOVED on banded maps.** Per-band is one flag away. Consequence: vanilla's strip is also what stops a colony sealing its map edge.\n**15e. Drop pods fall through the levels.** Suppress `Skyfaller.DrawAt`, re-issue via `DrawNowAt` translated by `(viewedBand - itsBand) * Slot`. Altitude chosen PER FRAME. `Skyfaller_FlyingPawn` excluded.\n\n# 16. ALTITUDE AND WEATHER (`ABBandWeather.cs`, verified)\nOne weather INTERPRETED per band, since `WeatherManager` is per-Map.\n- **Snow confined to real ground:** `AB_OpenAir` and `AB_WallTop` set `holdSnowOrSand=false`.\n- **Melt per band:** prefix on `DoCellSteadyEffects` rewrites the private `outdoorMeltAmount` on EVERY call including level 0.\n- **Snow must exist before it can persist:** `SeedAltitudeSnow` at generation, seeded from CURRENT temperature. ~35 ms, free.\n- **Rain falls and draws as snow above the line.** Accumulation via the public `AddFallenSnowAt`; vanilla gates on the cached field but takes the amount from the live property. Rejected: swapping `WeatherDef.Worker.overlays`, a global Def shared across maps.\n- **No overlays underground**, judged on the viewed band.\n- **Wind x1.15 / x1.30 / x1.50, zero underground.** Patched on the `DesiredPowerOutput` getter. `CalculateWindCells` spans 22 rows, gutter is 2.\n- **Readout:** `GlobalControls.TemperatureString` rebuilt from `TryGetTemperatureForCell`. `MapTemperature.OutdoorTemp` is NEVER reached by it, because outdoor cells still belong to a Room and `Room.Temperature` is a stored field.\n- **Rendering:** snow on a level below needs its own mirrored pass, §6f.\n\n# 17. SETTINGS PASS (verified)\n**Tabbed scrolling window:** Performance, Visuals, Climate, Sky, Basement, Diagnostics. Hand-drawn tab buttons, not `TabDrawer`. Each tab draws inside a try/catch that renders the exception into the pane.\n**⚠ The blank-tab bug.** Scroll-view content height measured from the previous frame's `list.CurHeight` is a SELF-SUSTAINING LATCH without a floor. Fix: `Mathf.Max(list.CurHeight + 16f, inner.height)`. Diagnosed by one label drawn OUTSIDE the scroll view. **Adding a tab means growing `viewHeights` too** - it is indexed by `(int)Tab`.\n**⚠ Draw the diagnostic OUTSIDE the thing being diagnosed.** A fallback error rendered inside a clipped region is silent, and its silence is not evidence.\n**The unlock gate.** The checkbox writes to a LOCAL; `unclampMapSize` only becomes true inside the confirmation callback, so the stored value IS the record that the warning was read. Red banner reports the multiple over budget, coloured against the BUDGET, not `Fits()`.\n**The budget stays a `const`**, deliberately.\n**Climate SNAPSHOTTED onto `ABBandMap` at generation** (`SnapshotClimate`, scribed, on BOTH carve paths). Order: snapshot, settings, hardcoded defaults.\n**A setting baked into VERTICES needs a rebake on change** (§6g shrink), done once in `ABMod.WriteSettings` rather than per slider frame.\n**Removed settings simply stop being scribed.** Old config files keep the unknown keys and RimWorld ignores them, so a downgrade is lossy but never an error (the Perspective Mode keys took this path).\n\n---\n\n# 18. CODE AUDIT, 2026-07-31 (ALL UNVERIFIED IN PLAY)\nFour read-only passes over ~19.8k LOC: hot paths, crash surface, adherence to this document's own rules, dead/duplicated code. Build clean, 0 warnings. The findings clustered into three shapes, and **all three were rules this codebase already states somewhere else**.\n\n## 18a. STATE TAKEN IN A PREFIX, RELEASED IN A POSTFIX (three instances, all fixed)\nHarmony does not run postfixes when the original method THROWS. Three patches used a postfix to put back something a prefix had taken:\n- `Patch_PawnRenderUtility_ABAimAngle` swaps `Stance_Busy.focusTarg` for the duration of a draw. That is LIVE COMBAT STATE, not render state, so one exception inside `DrawEquipmentAndApparelExtras` (a bad graphic, another mod's postfix) left a pawn permanently aiming at a band-translated cell. Worse, the prefix opened with `patched = null`, which DISCARDS a pending restore rather than deferring it.\n- `Patch_AdvancedGameConfig_ABMapSizeLock` latches `inChooser` and swaps the static `MapSizes` array. `inChooser` gates a patch on `Listing_Standard.RadioButton` - a primitive used by every mod's settings window - so a throw in VANILLA's body would relabel and DISABLE unrelated radio buttons for the rest of the session. A `finally` in the postfix does not help; it only covers our own body.\n- Both now release in a `Finalizer`, plus a restore-on-entry as belt and braces. Saved to user lore.\n\n**Unthrottled `Log.Error` on a hot path, same family, three instances.** The camera band clamp is a PER-FRAME postfix; `ABBandInput` runs per GUI event; the wormhole re-arm fires per region rebuild. A bare `Log.Error` there writes one line per frame until Player.log and the frame budget collapse - so the logging turns a cosmetic bug into a hang. All three now trip a guard switch (`Camera`, `Camera`, `Transit`), which also stops the throw recurring and offers a re-arm.\n\n## 18b. THE MIRROR (largest in-play win)\n`Patch_MapDrawer_ABMirrorDirtyUp` had two problems, both now fixed - see §6a for the mechanism.\n- **No `ProgramState` guard.** Vanilla's body returns immediately unless Playing, so during generation every mirrored call is a no-op - but the postfix ran anyway, dispatching (bandCount - 1) Harmony-wrapped re-entries per dirty call. `FillRock` issues two per cell across ~36k cells per basement band: order of 1.3M dead dispatches per 7-band carve.\n- **No ascent rule.** Climbed to every band regardless of whether any of them could see the changed cell.\n\n## 18c. DEAD AND DUPLICATED\n- **12 of 19 kill switches had no call site.** Deleted 11, kept and WIRED `Transit`, added `Camera`. Every remaining switch is referenced.\n- **The dev spike layout taxed every map in the game.** `ABBands.Banded/BandOf/SameBand` fell through to a second `ConditionalWeakTable` probe whenever the real component said no - which is the answer on every quest site, caravan map and unbanded colony - and `Banded()` is asked once per section regenerate from three patches. Now behind a `spikeCount == 0` fast-out.\n- **`ClearCellHard` was a second copy of `ClearCellExcept`** with nothing kept. Two destroy loops that must stay bit-identical (the geyser DeSpawn case, two profile counters) is how a census stops conserving.\n- **Two surface-edge finders** merged - see §15b.\n- **Five dead locals and one dead parameter** found by hand after the consolidation; the compiler does not warn on a local assigned from a method call, so each was fetching a grid nobody read.\n\n## 18d. NOT FIXED, DELIBERATELY\n- **Below-pawn drawing is serial and cache-disabled.** Vanilla batches `ParallelPreDraw` into a `ManagedJobParallelFor`; our below pass runs all three phases inline on the main thread, AND `Patch_PawnRenderer_ABBelowDisableCache` forces the expensive uncached path for exactly those pawns. Cost scales linearly with pawns visible below. The only remaining steady-state lever, and the hardest.\n- **Realtime-things descent and shrink** (§5, §6g). Feature work, needs a play run.\n- **Perlin precompute** (§9f item 4). Changes generation determinism.\n- **`ShotReport` target-size loss** (§13 item 13). Needs a transpiler.\n\n**⚠ Note for any future A/B on generation timing:** the `ScatterOres` rect overload draws a different NUMBER of `Rand` values than the old list overload, so ore placement for a given seed changed. Maps generated before and after 2026-07-31 are not comparable cell-for-cell.\n\n## TESTING RULE\n`isolated=true`, `quicktest=false` (the level chooser needs the menus; Map Preview doubly so).\nCompat tests need the target mod as a companion. Dubs (`dubwise.dubsmintminimap`), Map Preview (`m00nl1ght.MapPreview`) and VQE Ancients (`vanillaquestsexpanded.ancients`) are all INACTIVE in the user's real list. **VQE Ancients co-loaded clean in run #196, but added no gensteps and no census entries, so its colony-map surface was barely exercised; its content is likely quest-site based. `ScenParts` rose from 15.8 ms to 332.2 ms, the only sign it ran.**\nWater tests need a river or lake tile, coastline tests a coastal one. **Read the TILE line before comparing two runs.**\n**Re-check the level strip before generating.** The plan persists between colonies.\nRead the bisect line before trusting any water or snow observation.\nDepth-shrink tests need a populated level BELOW the viewing level, and must cross `ZoomRootSize` 18.\n**Perf tests must exercise the ACTIVITY, not the idle state** (§11): mine a large area with 3 sky levels open and watch the frame graph."
}